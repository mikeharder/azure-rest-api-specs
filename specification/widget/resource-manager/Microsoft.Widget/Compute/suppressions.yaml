- tool: TypeSpecValidation
  rules: [FolderStructure]
  paths: ["*"]
  reason: composite spec

- tool: TypeSpecValidation
  rules: [SdkTspConfigValidation]
  paths: ["tspconfig.yaml"]
  reason: customize sub-rules for each folder

- tool: TypeSpecValidation
  rules: [SdkTspConfigValidation]
  paths: ["Disk/tspconfig.yaml"]
  reason: customize sub-rules for each folder

- tool: TypeSpecValidation
  rules: [SdkTspConfigValidation]
  paths: ["Gallery/tspconfig.yaml"]
  reason: customize sub-rules for each folder
